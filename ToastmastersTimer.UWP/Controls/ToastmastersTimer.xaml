<UserControl
    x:Class="ToastmastersTimer.UWP.Controls.ToastmastersTimer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToastmastersTimer.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:ToastmastersTimer.UWP.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.DataContext>
        <viewModels:ToastmastersTimerViewModel></viewModels:ToastmastersTimerViewModel>
    </UserControl.DataContext>
    
    <Grid Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="90*"></RowDefinition>
                <RowDefinition Height="10*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid x:Name="TimerGrid" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="15"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid x:Name="MinutesGrid">
                    <Viewbox>
                        <TextBlock x:Name="MinutesText" Text="20" FontSize="50"></TextBlock>
                    </Viewbox>
                </Grid>
                <Viewbox Grid.Column="1">
                    <TextBlock Text=":" FontSize="150"></TextBlock>
                </Viewbox>
                <Grid x:Name="SecondsGrid" Grid.Column="2">
                    <Viewbox>
                        <TextBlock x:Name="SecondsText" Text="20" FontSize="50"></TextBlock>
                    </Viewbox>
                </Grid>
            </Grid>
            <StackPanel x:Name="ButtonsPanel" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 00 0 0" VerticalAlignment="Bottom">
                <Button x:Name="StartTimer" Click="{x:Bind ViewModel.StartTimer}" HorizontalAlignment="Center" Content="Start" ></Button>
                <Button x:Name="PauseTimer"  HorizontalAlignment="Center" Content="Pause"  Margin="15 0"></Button>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
