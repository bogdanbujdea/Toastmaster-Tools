<Page
    x:Class="ToastmastersTimer.UWP.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToastmastersTimer.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:ToastmastersTimer.UWP.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:HomeViewModel></viewModels:HomeViewModel>
    </Page.DataContext>

    <Grid Background="#004165">
       <StackPanel VerticalAlignment="Center">
            <Button Content="Timer" HorizontalAlignment="Center" Foreground="White" VerticalAlignment="Center" Click="{x:Bind ViewModel.GoToTimerView}" Margin="0 0 0 30"></Button>
            <Button Content="Settings" HorizontalAlignment="Center" VerticalAlignment="Center" Click="{x:Bind ViewModel.GoToSettings}"></Button>
           <!-- <Button Content="Practice Speech" HorizontalAlignment="Center" VerticalAlignment="Center" Click="{x:Bind ViewModel.GoToSpeechPractice}"></Button>-->
        </StackPanel>
    </Grid>
</Page>
